<h1>Comment</h1>
<% if user_signed_in? %>
  <%= simple_form_for [item, @comment] do |f| %>
    <%= f.input :comment %>
    <%= f.submit "Submit comment", class: "btn btn-primary" %>
  <% end %>
<% end %>
